<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.eldercare.mapper.AlarmMapper">
    <select id="findByElderlyId" resultType="com.example.eldercare.entity.Alarm">
        SELECT * FROM alarm WHERE elderly_id = #{elderlyId} ORDER BY created_at DESC
    </select>
    
    <select id="findByElderlyIdAndStatus" resultType="com.example.eldercare.entity.Alarm">
        SELECT * FROM alarm 
        WHERE elderly_id = #{elderlyId} 
        AND status = #{status} 
        ORDER BY created_at DESC
    </select>
    <select id="findByGuardianId" resultType="com.example.eldercare.entity.Alarm">
        SELECT a.* FROM alarm a
        JOIN binding b ON a.elderly_id = b.elderly_id
        WHERE b.guardian_id = #{guardianId}
          AND b.status = 1
        ORDER BY a.created_at DESC
    </select>
</mapper>