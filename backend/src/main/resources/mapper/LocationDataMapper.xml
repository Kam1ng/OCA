<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.eldercare.mapper.LocationDataMapper">
    <select id="findLatestByElderlyId" resultType="com.example.eldercare.entity.LocationData">
        SELECT * FROM location_data
        WHERE elderly_id = #{elderlyId}
        ORDER BY update_time DESC
        LIMIT 1
    </select>

    <select id="findHistoryByElderlyId" resultType="com.example.eldercare.entity.LocationData">
        SELECT * FROM location_data
        WHERE elderly_id = #{elderlyId}
        ORDER BY update_time DESC
        LIMIT 200
    </select>

    <select id="findByElderlyIdAndTimeRange" resultType="com.example.eldercare.entity.LocationData">
        SELECT * FROM location_data
        WHERE elderly_id = #{elderlyId}
        AND update_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY update_time ASC
    </select>
</mapper>