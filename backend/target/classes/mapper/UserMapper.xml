<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.eldercare.mapper.UserMapper">
    <select id="findByUsername" resultType="com.example.eldercare.entity.User">
        SELECT * FROM users WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectByPhone" resultType="com.example.eldercare.entity.User">
        SELECT * FROM users WHERE phone = #{phone} AND deleted = 0
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, phone, nickname) 
        VALUES (#{username}, #{password}, #{phone}, #{nickname})
    </insert>
</mapper>